#2. Write a Python program to convert Celsius to Fahrenheit?

def main():
    temp_c = 23
    temp_f = (temp_c * 9 / 5 ) + 32
    print (f"{temp_c} degrees Celsius is equivalent to {temp_f} degrees Farenhrit.")

if __name__ == "__main__":
    main()